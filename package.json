{
  "name": "yoink-ts",
  "description": "print a type into an existing file to avoid circular `any`",
  "type": "module",
  "version": "1.0.1",
  "author": "hasparus",
  "license": "ISC",
  "main": "dist/index.mjs",
  "bin": "dist/index.mjs",
  "scripts": {
    "build": "tsc -P tsconfig.json"
  },
  "devDependencies": {
    "@tsconfig/strictest": "^2.0.2",
    "@types/node": "^20.8.6"
  },
  "dependencies": {
    "typescript": "^5"
  }
}
